open Prelude importing (List, Maybe)

% domain: Carlos

# Chapter

@ Een item is iets wat we kunnen gebruiken
@ name: Item
@ value: Niet nodig
type Item =
    Key: String
    Value: String

@ api: Dit is ook een API
type Person =
    Name: String
    Something: Item
    Items: List Item

guideline {
    % title: Flows contain steps

    # Flows contain steps

    A flow should and must always contain steps. 
}

guideline {
    % title: Sometihng else
    % version: 0.1
    % author: Carlos Kelkboom

    We can just add a pragraph, and be happy with it!
}